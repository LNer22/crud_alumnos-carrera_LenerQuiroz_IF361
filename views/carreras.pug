extends base.pug


block title 
    title Carreras
    link(rel="stylesheet", href="bootstrap/bootstrap.min.css")
block header
    nav.navbar.navbar-expand-lg.navbar-dark.bg-dark
            .container-fluid
                a.navbar-brand(href='/') Tarea1_LenerQ
                .navbar-nav.justify-content-center
                    a.nav-link(href='/') Alumno
                    a.nav-link.active(aria-current='page' href='/carreras') Carrera
block container
    .container
        .row
            .col.mt-3
            p
                a.btn.btn-outline-success(href="/carrera/?accion=nuevo") Nueva Carrera
            .table-responsive
                table.table.table-hover.align-middle
                    thead 
                        tr
                            th(scope='col') Código
                            th(scope='col') Nombre
                            th(scope='col') Duración
                            th(scope='col') Módulos
                            th(scope='col') Formato
                            th(scope='col') Acción
                    tbody#data-carreras
block footer
    script.
        var data = !{JSON.stringify(data)};
    script(src="../js/carreras.js") 


